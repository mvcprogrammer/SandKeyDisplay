namespace Display.Core.Services.Bridge.Stellar.Enums;

public static class ParamEnums
{
    public enum SortDirection { None = 0, Ascending, Descending };
    public enum SortBy { None = 0, ListPrice };
    public enum ListingType { Invalid = 0, ResidentialSale, ResidentialLease };
}